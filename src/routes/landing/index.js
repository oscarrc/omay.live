import { BRAND } from "../../constants/strings";
import Terms from "../../components/terms";

const Landing = () => {
    return (
        <>
            <section className="flex gap-4 flex-col flex-1 w-full bg-base-100 rounded shadow-inner p-8">
            </section>
            <Terms show={true} />
        </>
    )
}

export default Landing;