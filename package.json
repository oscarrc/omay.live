{
  "name": "videochat",
  "version": "0.1.0",
  "description": "Random video chat",
  "scripts": {
    "start": "npm --prefix ./server start",
    "dev": "npm --prefix ./app start & npm --prefix ./server run dev",
    "build": "npm --prefix ./server install && npm --prefix ./app install && npm --prefix ./app run build",
    "translate": "./translate.sh"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oscarrc/videochat.git"
  },
  "author": "Oscar R.C.",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/oscarrc/videochat/issues"
  },
  "homepage": "https://github.com/oscarrc/videochat#readme",
  "devDependencies": {
    "@parvineyvazov/json-translator": "^2.2.1"
  }
}
